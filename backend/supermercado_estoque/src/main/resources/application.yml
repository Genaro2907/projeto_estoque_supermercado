spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/supermercado_estoque?useTimezone=true&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        formatsql: true
springdoc:
  paths-to-match: /api/**/**
  swagger-ui:
    use-root-path: true
  
##logging:
  #level:
  #  gabrieldev.com.supermercado_estoque: INFO
 # pattern:
 #   console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"